name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions-hub/docker@master
    - name: Try docker
      run: |
        docker -v
        docker-compose -v
